function orderNow() {
  alert("Thanks for your interest! Select an item from the menu to place your order.");
}

function orderItem(itemName) {
  alert(`✅ ${itemName} added to your order! (Feature under construction...)`);
}
